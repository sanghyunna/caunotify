// AUTO GENERATOR
const Major = [];
const major = [
    "russian",
    "french",
    "german",
    "philosophy",
    "history",
    "publicService",
    "civilEnvPlanEng",
    "urbanEngineering",
    "architecture",
    "appliedStat",
    "med",
    "pharm"
];
const len = major.length;
for(let i=0;i<len;i++){
    Major[i] = major[i].charAt(0).toUpperCase() + major[i].slice(1);
}

// ${major[i]}

for(let i=0;i<len;i++){
    console.log(``);
}
